package test.java;

import org.junit.Assert;
import org.junit.jupiter.api.Test;

import com.revature.projectreimbursement.models.Ticket;

class SampeRunTestNum2 {

	@Test
	void test() {
		// Arrange: set the variables (declarations and instantiations)
		//for the class/method/code that is being tested.
		/* course sample code
		 * 		int testUpVote = 4;
				Solution testSolution = new Solution();
		 */
		
		// Act & Assert:  We add the code that we are testing
		// and apply an Assert method: 
		//	Assert.assert.equals( var named above, instiation named above "dot" 
		//and then apply a try/catch block
		//The Assert forces the values we are testing for.
		// the catch block will house the Assert.testName(); we are running.
		/*course sample code
		 * 		try {
			testSolution.setUpvote(testUpVote);
			// Assert
			Assert.assertEquals(testUpVote, testSolution.getUpvote());
		} catch (Exception e) {
			Assert.fail();
		}
		 */

	}

}
